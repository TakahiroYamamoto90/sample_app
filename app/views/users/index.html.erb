<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= render 'search_form', url: users_path, q: @q %>
<%= paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= paginate @users %>